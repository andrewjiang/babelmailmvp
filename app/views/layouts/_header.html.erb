<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header babel-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#babel-navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand babel-brand" href="/"><%= image_tag "babel-small.png", class: "babel-brand-img" %> Babel Mail</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="babel-navbar-collapse">
      <ul class="nav navbar-nav" id="babel-main-links">
        <li><a href="#">How it works</a></li>
        <li><a href="#">Languages</a></li>
        <li><a href="#">Pricing</a></li>
      </ul>
      <ul class="nav navbar-nav pull-right">
      <%= javascript_include_tag "dropdown_menu" %>
          <% if user_signed_in? %>
            <%= render 'layouts/navbar_signed_in' %>
          <% else %>
            <%= render 'layouts/navbar_signed_out' %>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<!--nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="row">
      <div class="col-sm-4 col-sm-offset-4 col-xs-12" id="logo">
        <center><%= link_to "ZEPHYR", root_path, id: "logo" %></center>
      </div>
      <div class="col-sm-4 col-xs-12" id="header-right">
        <center>
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>

          <%= javascript_include_tag "dropdown_menu" %>
          <% if user_signed_in? %>
            <%= render 'layouts/navbar_signed_in' %>
          <% else %>
            <%= render 'layouts/navbar_signed_out' %>
          <% end %>

          </div>
        </center>
      </div>
    </div>
  </div>
</nav-->
<div style="width:100%;">
<% flash.each do |key, value| %>
  <% if key == :notice %>
    <div class="alert alert-success ephemeral"><%= value %></div>
  <% else %>
    <div class="alert alert-danger ephemeral"><%= value %></div>
  <% end %>
<% end %>
</div>
